{
  "name": "whatsapp-transport-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/conversation.js && node --check src/whatsapp.js && node --check src/sheets.js && node --check src/storage.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
